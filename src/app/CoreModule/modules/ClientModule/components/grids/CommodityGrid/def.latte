{* ====== FILTER ============================= *}

{define filter-col-name}
	{input name, class => 'form-control'}
{/define}

{define filter-col-info}
	{input info, class => 'form-control'}
{/define}

{* ====== LINE =============================== *}

{define col-name}
	<em>{$row->name}</em>
{/define}

{define col-parent}
	{$row->parent ? $row->parent->name : '--'}
{/define}
